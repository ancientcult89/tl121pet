@model SimpleEditFormVM<MeetingType>
<partial name="_Validation"></partial>

<h5 class="bg-@Model.Theme text-white text-center p-2 rounded-top">@Model.Mode.ToString()</h5>
<div class="shadow-lg p-3 mb-5 bg-white rounded">
    <form asp-action="@Model.Mode" method="post">
        @if (@Model.Mode == FormMode.Edit)
        {
            <div class="form-group">
                <label asp-for="SelectedItem.MeetingTypeId"></label>
                <input class="form-control" asp-for="SelectedItem.MeetingTypeId" readonly/>
            </div>        
        }
        <div class="form-group">
            <label asp-for="SelectedItem.MeetingTypeName"></label>
            <div>
                <span asp-validation-for="SelectedItem.MeetingTypeName" class="text-danger"></span>
            </div>
            <input class="form-control" asp-for="SelectedItem.MeetingTypeName" disabled="@Model.IsReadonly"/>
        </div>

        @if (Model.Mode != tl121pet.Storage.FormMode.Details)
        {
            <button class="btn btn-info mt-2" type="submit">
                Save
            </button>       
        }
        <a class="btn btn-secondary mt-2" asp-action="MeetingTypeList">Back</a>
    </form>
</div>